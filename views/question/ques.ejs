<html>
    <head>
        <title>
            Arrays-CodeWorld
        </title>
    </head>
    <style>
               	.navbar {
  overflow: hidden;
  background-color: #333;
  position: fixed;
  top: 0;
  height:80px;
  width: 100%;
  line-height: 40px;
  vertical-align: middle;
  z-index: 1;
}

.navbar button, input[type="submit"], input[type="reset"] {
	background: none;
	color: inherit;
	border: none;
	padding: 0;
	font: inherit;
	cursor: pointer;
	outline: inherit;
}

.navbar a ,.navbar button {
  float: left;
  display: block;
  color: #f2f2f2;
  text-align: center;
  padding: 14px 16px;
  text-decoration: none;
  font-size: 20px;
}

.navbar a:hover,
.navbar .active, .navbar button  {
  background: #ddd;
  color: black;

}

*, *:before, *:after {
  -moz-box-sizing: border-box;
  -webkit-box-sizing: border-box;
  box-sizing: border-box;
}

body {
  font-family: 'Nunito', sans-serif;
  color: #384047;
}

table {
  width: 1060px;
  margin: 200px auto;
 
}

caption {
  font-size: 1.6em;
  font-weight: 400;
  padding: 10px 0;
}

thead th {
  font-weight: 400;
  background: #8a97a0;
  color: #FFF;
  text-align:center;
}

tr {
  background: #f4f7f8;
  border-bottom: 1px solid #FFF;
  margin-bottom: 5px;
 
}

tr:nth-child(even) {
  background: #e8eeef;
}

th, td {
  text-align: left;
  padding: 20px;
  font-weight: 300;
  text-align:center;
}

tfoot tr {
  background: none;
}

tfoot td {
  padding: 10px 2px;
  font-size: 0.8em;
  font-style: italic;
  color: #8a97a0;
  
}

    </style>
    <body>
        <%- include('../includes/navigation.ejs') %>
        
        <table>
            <caption><%= topic %></caption>
            <thead>
              <tr>
                <th scope="col" colspan="10"> Question Link</th>
               
                
              </tr>
            </thead>
            <tfoot>
              <tr>
                <td colspan="3"><a href="/problem/add/<%=params %>" style="font-size:large">Add Questions</a></td>
              </tr>
            </tfoot>
            <% if(ques.length > 0) { %>
              <tbody>
              <% for(let i=ques.length-1; i>=0; i--) { %>
                <tr>
                  <th scope="row" colspan="10"><a href="<%=ques[i].link %>"><%=ques[i].link %></a></th>
                  
                  
                </tr>
                <% } %>
              </tbody>
              
              <% } else { %>
                <tr>
                  <th scope="row" colspan="10">No questions yet.. :(</th>
                
                </tr>
                <% } %>

          </table>



    </body>
</html>



